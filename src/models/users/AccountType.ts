/**
 * @file Declares AccountType enum representing type of account associated with each user entity.
 */

/**
 * @typedef AccountType Represents an account type associated with a user.
 */
enum AccountType {
    Personal = "PERSONAL",
    Academic = "ACADEMIC",
    Professional = "PROFESSIONAL"
}

export default AccountType;